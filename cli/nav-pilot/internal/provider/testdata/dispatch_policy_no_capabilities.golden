# Local worker on this machine

The `local-worker` agent runs on mlx-community/Some-Model-4bit here on the machine. It draws no AI credits: everything it generates is free, however many tokens it takes. That is the whole reason to send anything to it.

What the model was chosen for: Sub-agent worker
What it actually delivers: Returns in seconds.

Send it: lookups in the code, comments, log lines, a single test file, and mechanical changes that follow one pattern. A rename hits call sites in several files and still belongs there.
Describe the change fully when you send it: which file, which line, what it becomes. The model carries out a decision well and makes one badly, so if you doubt it can do the task, do it yourself. The measurements say you judge this correctly.
Do not send it: changes needing a judgement per file, tasks needing many rounds, changes where a wrong edit is expensive.

It usually answers in seconds, but a single token has been measured at three and a half minutes under load. The client gives up on its own after 10 minutes without an answer, so wait for it. Interrupting earlier can duplicate a change that is still in flight. Send one task at a time: the model runs on one GPU, so concurrent calls get nothing done faster.

It fails in two ways. Both are cheap to spot, and both mean you take the task yourself rather than sending it again:
- It often says no and changes nothing. Check the file actually changed. If it did not, you lost a few seconds and no credits.
- It can repeat the same tool call. nav-pilot ends the turn after 3 identical calls in a row that got the same result back, or after 5 identical calls whatever they return.
