name: nav-pilot CI

on:
  push:
    branches: [main]
    paths:
      - 'scripts/install-collection/**'
      - 'scripts/install.sh'
      - '.github/skills/**'
      - '.github/agents/**'
      - '.github/collections/**'
      - '.github/instructions/**'
      - '.github/prompts/**'
      - '.github/workflows/nav-pilot-ci.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'scripts/install-collection/**'
      - 'scripts/install.sh'
      - '.github/skills/**'
      - '.github/agents/**'
      - '.github/collections/**'
      - '.github/instructions/**'
      - '.github/prompts/**'
      - '.github/workflows/nav-pilot-ci.yaml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build and test installer
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version-file: scripts/install-collection/go.mod
          cache: true

      - name: Build
        run: cd scripts/install-collection && go build -o nav-pilot .

      - name: Test
        run: cd scripts/install-collection && go test -race -v ./...

      - name: Vet
        run: cd scripts/install-collection && go vet ./...

      - name: Staticcheck
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          cd scripts/install-collection && staticcheck ./...

  shellcheck:
    name: Lint install.sh
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: ShellCheck
        run: shellcheck scripts/install.sh
